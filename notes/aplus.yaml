dmrec:
- string: '' # seemed that this wasn't parsed to JSON!?!
- regex: ^$
file:
- method:
  - check_filenames_assets
  which: item
title:
- regex: '^Complex Object: .*$'
  which: complex
- regex: '^Complex Object Item: .*$'
  which: item
